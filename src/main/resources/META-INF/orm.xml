<?xml version="1.0" encoding="UTF-8"?>
<entity-mappings
    xmlns="http://xmlns.jcp.org/xml/ns/persistence/orm"
    version="2.2">
    <named-query name="Restaurant.findAllFreeDelivery">
        <query>
            FROM Restaurant r
            JOIN FETCH r.cuisine
            WHERE r.deliveryFee = 0
        </query>
    </named-query>

    <named-query name="Product.findPhotoById">
        <query>
            SELECT pp
            FROM ProductPhoto pp
            JOIN pp.product p
            WHERE p.restaurant.id = :restaurantId
            AND p.id = :productId
        </query>
    </named-query>
</entity-mappings>